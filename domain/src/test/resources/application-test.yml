spring:
  r2dbc:
    url: r2dbc:postgresql://localhost:5432/testdb
    username: test
    password: test
  sql:
    init:
      mode: always
      schema-locations: classpath:/schema/test_db_schema.sql
